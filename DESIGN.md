# yo-skill 设计系统（DESIGN.md）

> 从已建成的 7 屏原型（`prototype/`）归纳的耐用视觉决策。事实以代码为准；改动原型前先读这份，改完同步回来。
> 方向：品类标准（canon），用户于 2026-08-11 经 standing exit 选定；质感对标 1Password（信息架构）× Raycast（单一入口）× Linear（分层与精致度）。seed 17bf85bd。
> 视觉定稿 V2：**E 墨极**（黑白极简中性栈），用户于 2026-08-11 从 5 套候选（深空靛/深翠/石墨蓝/珊瑚赭/墨极，含竞品参考与浅/深双模式）中选定，替换薄荷绿旧案；候选对比存档于 `prototype/themes.html`。
> 同日 V3 修订（用户改定）：行动层从纯黑白改为**功能色**——可点行动 / 成功 / 危险分色，琥珀橙维持提醒语义；黑白骨架不变。
> 同日 V4 定稿（用户改定）：功能色色相定为**翡翠**（Radix jade 同族）——Tailwind 默认蓝绿红显廉价、蓝太普通、紫"AI 味"重，三轮对比后选定；成功徽章中性化（中性灰底 + 翡翠勾/图标，避免与主色双绿打架），`--ok` 绿退居同步点等微小语义位；成功绿 / 危险红取 Radix 低饱和系。候选对比存档于 `prototype/colors.html`。

## 色彩（品牌绑定，不可改）

黑白骨架 + 四色功能语义：交互翡翠 = 一切可点行动，绿 = 同步点等微小成功语义（徽章已中性化），红 = 危险操作，琥珀橙仅承担"需要照料"的提醒。深色模式独立设计，非浅色反相。

**浅色模式**（`:root`）

| 角色 | 值 | 用途 |
|---|---|---|
| 纸面 `--paper` | `#FAFAFA` | 全局底色（zinc-50） |
| 纸面深 `--paper-deep` | `#F0F0F1` | 侧边栏、kbd、version-tag 底 |
| 卡片白 `--card` | `#FFFFFF` | 所有卡片、搜索框、面板 |
| 墨黑 `--ink` | `#18181B` | 主文字（zinc-900） |
| 墨黑 2/3/4 | `#3F3F46` / `#71717A` / `#A1A1AA` | 次级/辅助/占位文字（正文对比度 ≥4.5:1）|
| 行动 `--accent` | `#29A383`（hover `#26997B`，墨 `#208368`） | **仅**主行动按钮、开关开、焦点环、激活下划线/描边、链接态 |
| 行动文字 `--on-accent` | `#FFFFFF` | 行动色底上的文字 |
| 行动软底 `--accent-soft/-softer` | `#E6F7ED` / `#F2FAF6` | 图标软底（tone-mint）、focus 光晕、选中底色 |
| 成功 `--ok` 系 | `#30A46C`（墨 `#218358`，软底 `#E6F6EB` / `#F2FBF5`） | 仅同步点等微小语义位；已安装/已同步徽章用 `--neutral-soft` 灰底 + `--accent` 翡翠勾 |
| 危险 `--danger` 系 | `#E5484D`（墨 `#CE2C31`，软底 `#FEEBEC`） | 卸载/移出类 quiet 按钮 |
| 琥珀 `--warn` | `#E8890C` | **仅**提醒：可更新点、重复/冲突提示、需要 API Key 徽章 |
| 琥珀墨 `--warn-ink` | `#8A4E04` | 琥珀软底上的文字 |
| 描边 `--hairline` | `#E4E4E7` | 所有 1px 分隔 |

**深色模式**（`[data-mode="dark"]`，功能色提亮一档）

| 角色 | 值 | 用途 |
|---|---|---|
| 纸面/纸面深 | `#0A0A0B` / `#131314` | 底/侧边栏 |
| 卡片 `--card` | `#18181A` | 抬升面（亮于底色，非投影堆叠） |
| 墨白 `--ink` | `#FAFAFA` | 主文字 |
| 墨白 2/3/4 | `#D4D4D8` / `#8E8E96` / `#5C5C64` | 次级/辅助/占位 |
| 行动 `--accent` | `#27B08B`（墨 `#1FD8A4`，按钮白字 `--on-accent: #FFFFFF`） | 同浅色角色 |
| 行动软底 | `rgba(39,176,139,.18)` / `rgba(39,176,139,.10)` | 同浅色角色 |
| 成功 `--ok` 系 | `#33B074`（墨 `#3DD68C`，软底 rgba 绿） | 同浅色角色（徽章同样中性化） |
| 危险 `--danger` 系 | `#EC5D5E`（墨 `#FF9592`，软底 rgba 红） | 同浅色角色 |
| 琥珀 `--warn` | `#F0A63C`（文字 `#F5C886`，软底 `#332A15`） | 同浅色角色 |
| 描边 | `#27272A` / 强 `#3F3F46` | 分隔 |

纪律：四色只做功能语义、不做装饰性铺色——**可点行动只用 `--accent` 翡翠系**（实心为主、描边为次），**危险操作只用 `--danger` 红系**（仅 quiet 级），琥珀只出现在非按钮的提醒标记上；**成功态徽章一律中性化**（`--neutral-soft` 灰底 + `--accent` 翡翠勾/图标，经 `.mark` 或 svg 着色），`--ok` 绿只留同步点这类无需阅读的微小语义位——全局绿色只以主色翡翠一种出现，避免双绿打架。开关旋钮独立变量 `--knob`（浅色白、深色墨黑，避免彩轨白钮看不清）。Agent 身份标记用**真实品牌 logo 官方配色、不铺底色**（2026-08-12 用户改定：品牌色瓷砖底废弃——之前学 cc-switch 的彩色瓷砖识别；字母/色块占位早已废弃）：官方 SVG 存 `logos/`（simple-icons + 各官网/官方仓库，本地引用无依赖），`.agent-logo`（34px 容器）/ `.agent-logo-xs`（13px 迷你）/ `.platform-icon img` 三档；瓷砖墙与"当前助手"行内 logo 直接以官方配色呈现、容器透明（glyph 放大到 26px 补视觉重量），品牌彩色 logo（Claude 珊瑚、Gemini 紫、Trae 绿、Qwen 紫、Replit 橙）原样呈现，纯黑 logo 在瓷砖墙**内联 SVG 也以官方原版色呈现**（2026-08-12 用户再次改定：tone 品牌向改色与深色 `.logo-invert` 反白一并废弃——Codex/Cursor/Copilot/Kimi/Continue/Roo/Aider 官方黑 `#000000`、Windsurf `#0B100F`、Cline `#18181B`；file:// 下 mask 子资源会失败的教训，内联最稳；Roo Code 用官方字标裁出的袋鼠图标）；**深色模式下纯黑 logo 垫中性浅色圆角底托**（`#F4F4F5`，经 `svg[fill]` 属性选择器命中、HTML 零改动，彩色 logo 无底托，迷你档与列表 chip 同规则）；身份色不占用状态语义色。`chip-mint` / `tone-mint` / `btn-outline-mint` 等类名是薄荷时代的历史命名，类名保留不改。

## 字体

- 英文/数字 **Inter**（品牌定稿绑定；detect.mjs 的 overused-font 警告已知悉并豁免），中文 **思源黑体（Noto Sans SC）**，Google Fonts CDN 引入
- 字阶：12（辅助）/ 12.5-13.5（正文与徽章）/ 15-17（卡片名与分组题）/ 22-26（页题）/ 750-800 字重给标题，650-700 给强调
- 标题 `letter-spacing: -0.02em`（页题 -0.025em），不超过 -0.04em 下限
- 数字一律 `font-variant-numeric: tabular-nums`（版本号、计数、星标数）

## 形状与深度

- 卡片圆角 14px（`--radius`），小控件 9px（`--radius-sm`），徽章/开关用 pill
- 高度只声明一次：卡片 = 1px `--hairline` 描边、无投影；hover 才允许 `translateY(-2px)` + 偏移柔阴影（浅色 `0 6px 20px -8px rgba(15,20,25,.14)`，深色 `0 8px 24px -8px rgba(0,0,0,.6)`）
- 深色模式的层级靠明度阶梯（纸面 < 侧栏 < 卡片）而非投影
- 禁止硬偏移投影、渐变文字、装饰性玻璃拟态

## 布局

- 侧边栏 232px（`--paper-deep` + 右描边，sticky），导航项 ≤3：已安装 / 发现；设置沉底；同步状态做成行动色软底小胶囊；浅/深切换钮（月亮/太阳）在设置上方
- 主区 max-width 1040px，padding 36/44；商店「发现」页卡片网格 3 列、间距 14px（浏览形态）；主屏「已安装」Skills/MCP 用行式列表（管理形态，条目多了也好扫）
- 分组标题 17px + 计数灰字，组间距 40px，标题上方留白大于下方
- 主屏信息架构（2026-08-12 再改版）：搜索框 → 重复项提醒条 → **页面级视图切换「按助手看 | 统一视图」**（`.view-switch`；2026-08-12 用户改定：**按助手看为默认落地**（瓷砖墙优先），统一视图退为第二个标签）→ 按助手看 =「我的 AI 助手」瓷砖墙 + 当前助手面板（面板内 Skills / MCP 分段切换）；统一视图 = 跨助手按名合并的去重列表，内部同样按 Skill / MCP 分段切换（一页只看一组，分段计数 + 琥珀点，MCP 不被几十条 Skill 列表压到屏外）；跨助手重复项由提醒条进 dedupe 页统一处理

## 组件约定

- **全局搜索框**置顶（白卡 + 行动色 focus ring + `⌘K` kbd 提示）——Raycast 式单一入口
- **Agent 瓷砖墙**（2026-08-12 用户改定：横向拖动条带废弃——助手多了不用拖半天）：换行网格（`.agent-grid` auto-fill minmax 152px），紧凑瓷砖 = 官方配色 logo（无底色）+ 名称 + 版本/更新状态，有更新的瓷砖右上角挂琥珀小圆点；点选 / Enter/Space 切换，选中 = 1px 行动色描边；**超过 12 个自动出现过滤框**（即时按名称过滤）；版本与更新动作收进瓷砖墙下方的**"当前助手"行**（`.agent-current`：logo + 名称 + 版本与 Skill/MCP 数 + 更新按钮或"已是最新"徽章，JS 随切换填充）；支持 `?agent=codex` URL 直达；演示 15 个主流助手（Claude Code / Codex / Gemini CLI / Cursor / Windsurf / GitHub Copilot / Kimi Code / Cline / Roo Code / Continue / Aider / Trae / Qwen Code / OpenCode / Replit，前 7 个有静态面板，后 8 个由 JS 以演示数据现场生成面板）；**区分已安装 / 未安装**（2026-08-12 用户改定）：已安装瓷砖原样在前，未安装瓷砖 `is-absent` 整体灰化（彩色 logo `grayscale(1)` + 降透明度、副文案改"未安装"）排在后，中间 `agent-divider` 分隔线标注"还没装 · N 个"；点未安装瓷砖不展示列表，当前行显示"未安装 + 一键安装"按钮、面板显示安装卡（`.install-empty`：一键安装为主行动、"去官网看看"描边次要行动 + 安装方式说明）；**一键安装流程**（2026-08-12 新增，学 cc-switch 应用内装工具）：点击 → 进度态（`.install-progress` 翡翠进度条 + "下载→安装→验证"步骤文案，约 2.5s）→ 成功态（翡翠勾 + "开始管理"）；完成后瓷砖**翻转成已安装**（去灰化、补版本号、挪到分隔线前、分区计数与分隔线文案同步刷新），localStorage 记住已装状态、刷新不丢；每个未装助手的版本与安装方式说明存 `INSTALL` 演示数据（npm / pip / VS Code 扩展 / 官方安装包 / 一键脚本）；整墙在「按助手看」视图（2026-08-12 用户改定：按助手看为默认落地），支持 `?agent=` URL 直达
- **统一视图**（`[data-view]` 双层容器 + JS 聚合，2026-08-12 新增）：视图切换的第二个标签（2026-08-12 用户改定：按助手看为默认落地），跨助手**按名合并** Skill/MCP 去重——内部再按 **Skill / MCP 分段切换**（`data-uni-tab`/`data-uni-panel`，一页只看一组，分段计数 + 琥珀点与按助手面板同约定；MCP 不被几十条 Skill 列表压到屏外，2026-08-12 用户反馈修订），计数文案"M 条 · 去重后 N 个"；跨助手条目带"N 个助手"chip（最多 3 个迷你 logo `.agent-logo-xs` 叠放）；聚合数据源 = 静态面板 DOM + `window.YO_EXTRA` 演示数据（只收已安装助手），装新助手后自动重建（`yoRebuildUnified`）；MCP 聚合态取 fail>ok>off，行首状态点与整行置灰沿用单助手规则；列表工具条同样 >12 条才出现，已改为可重绑定（`yoInitToolbars`，行快照存 `bar._rows`、事件只绑一次）
- **分组切换**（`.subtabs`，2026-08-12 新增）：助手面板内 Skills / MCP 分段控件——`--paper-deep` 底圆角容器 + 选中白卡浮起，标签带计数胶囊（选中时染翡翠软底），组内有"有新版本"行时 JS 自动挂琥珀小圆点（`.subtab-dot`）；一页只渲染一组，MCP 始终一键直达
- **配置位置条**（`.config-bar`，2026-08-12 新增）："当前助手"行下方一条窄条，亮出该助手的 **Skill 目录**与 **MCP 配置文件**真实路径（等宽字体 `.config-path`，如 `~/.claude/skills/`、`~/.codex/config.toml`），各带"复制"按钮（点击写剪贴板 + 短暂"已复制"反馈）；路径存在瓷砖 `data-skill-path` / `data-mcp-path` 属性上由 JS 随切换填充，**未安装助手整条隐藏**
- **MCP 连接状态**（`.status-dot`，2026-08-12 用户改定）：状态挪到**行首名称前一个小圆点**——已连接 `--ok` 绿 / 未启用灰（`--hairline-strong`）/ 失败 `--danger` 红，正常状态不带任何文字（沿用"微小语义位"纪律）；**只有失败才在行尾显示原因**（`.conn-fail` 红点 + `--danger-ink` 红字，如"连接超时""启动失败"）；**开关关掉的 MCP 整行置灰**（`.list-row.is-off`：图标降透明度、名称/说明/chip 降灰，状态点同步变灰）；开关可点（事件委托），切换时行置灰与状态点联动，失败行重新打开仍回红点；静态面板与 JS 生成面板同一套标记（`rowMcp` 第 4 参指定失败态）
- **内容识别色层**（2026-08-12 用户改定，学 cc-switch 彩色图标）：Skill/MCP 条目图标按**名称哈希固定分配** 8 色（`tone-mint/blue/iris/purple/tomato/orange/teal/ink`，`yo.js` 全站自动着色），同名条目在任何助手、任何页面同色；色值取 Radix Colors 低饱和系（bg=3 / ink=11），深浅两态独立。**与功能语义色分层**：翡翠/琥珀/红仍只表达"可点/提醒/危险"，图标色只是识别色、不含语义——四色功能纪律不受此层影响
- **已安装列表**：`.list` 行式列表——行内 图标 + 名称/说明 + 行尾状态区（更新提醒 / 徽章 / 开关），行间 1px hairline，整行可点进详情；仅跨助手条目显示"N 个助手"chip；数量级到几十上百条仍一屏可扫
- **列表工具条**（2026-08-12 新增，应对 50+ 条目）：设计约定**单个分组超过 12 条才出现**，少了不打扰——即时过滤框（匹配行内全部文字，含徽章、"需要 API Key"等状态词）、排序下拉（默认"有更新优先"，可切"名称 A→Z"）、右侧计数（过滤时显示"N / 总数"，有更新数用琥珀色标出）；演示见主屏 Claude Code 面板（16 Skill / 6 MCP）
- **商店「发现」页**（2026-08-12 方案 A 真交互改版）：顶部 Skill / MCP **分段切换**（同主屏 `.subtabs` 约定，带计数，一页只看一组，MCP 不被 Skill 卡片压到下方）；其下分类 tabs（推荐 / 写作 / 编程 / 设计 / 办公 / 生活）**真过滤** + 即时搜索框 + 排序下拉（最受欢迎 / 最新 / 评分）+ 右侧"N / 总数"计数；无结果显示 `.store-empty` 空态文案；卡片（`.card-grid` 3 列）= 识别色图标 + 名称/作者 + 一句话 + 平台图标（**联动"我的助手"**：已装助手彩色在前、未装 `.platform-icon.dim` 灰显在后，超 4 个叠 "+N"）+ 星标 / 已扫描 + MCP 卡"需要 API Key"琥珀徽章；已装条目显示"已安装"中性徽章（localStorage `yo-store-installed` 记忆）；**安装弹层**（`.pop-mask` + `.pop`）：点安装 → 选助手（已装 9 个默认全选，checkbox 行）→ 进度（翡翠进度条 + "下载→安装→验证"三步，约 2s）→ 成功态（翡翠勾 + "装好了"，1.1s 自动关）→ 卡片即时翻成已安装徽章；Skill 卡片本体可点进 `skill.html` 详情（带 `?name=&type=&from=store` 参数数据驱动重建，含**源 README** 面板——markdown 排版（h1/h2/引用条/代码块），MCP 条目附 `mcpServers` 配置示例；商店上下文隐藏更新/卸载按钮与"已安装到这些助手"面板，MCP 无"存放方式"概念同步隐藏；需 Key 条目挂琥珀 `.badge.warn`）；演示数据 JS 化（Skill 24 条 / MCP 12 条，含名称/作者/分类/星标/日期/评分/支持助手/推荐位/需 Key 字段）；⚠️ 工程教训：自定义 `display` 的弹层容器必须补 `.pop-mask[hidden]{display:none}`，否则 CSS 会盖掉 `hidden` 属性导致空弹层常显
- **设置页**（2026-08-12 新增，参考 Cindy `SettingsView` 分区架构）：主区左侧 172px 竖排分区 tab（`.settings-nav` sticky：通用 / 云同步与设备 / API Key / Skill 存放 / 关于，图标 + 文字，选中白卡浮起），右侧 section（`.panel`）纵向堆叠，切换 **150ms 淡入**（`.spanel.fade-in`，Cindy 纪律）；行式控件 `.set-row`（左 名称+说明、右 `.set-ctrl` 控件组，行间 hairline）；**外观三选一**（浅色/深色/跟随系统，`yoSetMode('system')` = 清除记忆跟随系统，与侧栏切换钮同一数据源 `yo-mode`）；偏好开关默认值：开机自启关 / 自动更新开 / **遥测关**（话术"默认关闭，不开也能正常用全部功能"，对应 telemetry no-op 纪律）/ 云同步开；云同步区 = E2E 状态行（"云端只存密文"话术）+ 我的设备（当前设备 chip + 移除演示）+ **配对码添加设备**（6 位等宽大数字、10 分钟有效、复制/重新生成，device-link UX 借鉴 Cindy）；**API Key 保险库** = AES-256 说明行 + 需 Key 的 MCP 逐行（已填写 `chip-mint` 遮蔽值 / 未填写 `chip-orange`，password input + 眼睛切换明文/遮蔽 + 保存即时生效）；Skill 存放区 = 复用 `.place-option` 全局默认（**只影响新装，已装的更新不静默改模式**）+ 通用目录 `~/.agents/skills/` 路径复制；关于区 = 版本卡 + 检查更新演示 + 筹备中条目（`.set-soon`）+ **危险区**（两步确认清除全部 `yo-*` localStorage 演示数据并刷新，原型复位用）；分区状态记 `yo-settings-tab`，支持 `?tab=` 直达；全部演示状态走 localStorage，页脚统一标注
- **向导页**（`.ob-*`，onboarding.html / restore.html，2026-08-12 新增）：首次收拢（旅程 1）与换电脑恢复（旅程 4）两个**无侧栏全屏向导**共用——居中窄栏 600px（`.ob-wrap`）+ 品牌字标 + 步骤点（`.ob-step-dot`：active 翡翠 + 软光晕 / done 翡翠 / 未到灰）；页题 24px 居中 + 副文案；扫描日志逐行浮现（`.ob-scan-line` 复用 setFade 带 delay）；进行中复用安装卡进度组件（`.install-track/.install-bar` + 步骤文案轮换），成功勾复用 `.install-done-icon`（向导放大档 `.ob-done-icon-lg`）；汇总数字用统计卡 `.ob-stat`（23px 大数字 tabular-nums + 灰注）；主行动居中 230px 宽，次级降级 `.ob-quiet` 文字链；云同步 A/B 复用 `.place-option`，恢复码复用 `.pair-code`；旅程 1 顺序 = 扫描 → 发现 → 一键收拢 → 云同步（**先给价值再要账号**），旅程 4 = 解锁 → 一键恢复 → 结果清单成功态（核心付费"值了"瞬间）；向导不写 localStorage，纯演示动画
- **按钮**：主按钮翡翠底白字（`--accent` / `--on-accent`）；次级行动（如"更新到 vX"）用翡翠描边 `btn-outline-mint`；ghost 描边给次选（含"两个都留"）；quiet 红字仅给"卸载"类低频危险动作
- **已安装态**（已安装/已同步徽章）：中性灰底徽章（`--neutral-soft`）+ 翡翠勾/图标，无 hover、不可点，与翡翠行动按钮明确区分
- **开关**：38×22 自定义 switch，开=交互翡翠，关=灰，旋钮色独立 `--knob`
- **存放方式选择**（`.place-option`，2026-08-12 用户定稿，依据 `mavis-deep-research/20260812_115055_skill_storage_strategy/`）：skill 详情页的"模式 A/B"单选卡——选中 = 1px 行动色描边 + 3px `--accent-softer` 光晕 + 右上圆形勾（未选中空心圈），"推荐"标用翡翠软底小胶囊；话术只有"所有助手共用一份（推荐）/ 每个助手单独放一份"，**界面不出现 symlink/junction/copy**；切换联动"已安装到这些助手"面板的生效方式 chip（"共用目录直接生效"用翡翠 chip，"已搭通道"/"单独副本"用中性 chip）与说明文案；全局默认在设置（原型仅文案提示）
- **能力徽章**：pill 描边徽章（会联网/会读文件）+ 中性底徽章（已扫描/自包含/不需要 API Key，图标与勾用翡翠着色）
- **更新提醒**：琥珀小圆点 + 3px 琥珀软光晕，文字用 `--warn-ink`，禁整卡橙边
- **重复项组**：实例行用纸面底圆角条（装在哪·何时装·用得多少），推荐动作=行动主按钮，其余降级；已处理组整组沉成 `--paper-deep` 中性底，右侧"已处理"flag 翡翠勾 + 灰字；2026-08-12 真交互化：组注册表与处理结果存 `yo-dedupe.js` + localStorage `yo-dedupe`（dedupe 页与主屏提醒条共用同一份状态，跨标签页 storage 事件同步），点击即翻转已处理态（带「撤销」可回退演示），主屏提醒条数字 = 未处理组数实时联动、全部处理完整条隐藏，设置页"清除全部数据"可复位
- **折叠区**（试运行/原始定义）：默认收起，右侧灰字标注"高级"
- **浅/深模式**：`yo.js` 控制 `<html data-mode>`，localStorage 记忆 > URL `?mode=` 参数 > 跟随系统；侧栏底部切换钮图标与文案随模式互换
- **演示数据**每页页脚统一标注

## 动效

仅一处编排式动效：主内容卡片 `fadeUp`（10px 上浮 + 渐显，0.55s 指数缓出， stagger 50ms）。开关/链接/按钮用 150-200ms 状态过渡。不加滚动特效。

## 文案

界面直接用通用名词：AI 助手 / Skill / MCP / API Key，导航为 已安装 / 发现（2026-08-11 用户改定，原 大脑/工具箱/保险库/钥匙 隐喻体系已废弃）。按钮是动词（安装 / 更新 / 合并成一份），错误说人话+给出路。禁用词表见 PRODUCT.md。

## 浏览器表面

选中色 = 行动色软底；focus-visible = 2px 行动色环；滚动条 10px 灰圆角；光标色 `--accent-deep`；placeholder 用 `--ink-3`（不用更浅的 ink-4，对比度不达标）。

## 待补

- logo 仅文字标 `yo` 行动色方块占位，正式 logo 待定
- 移动端布局仅 980px 降级一档，非目标平台
